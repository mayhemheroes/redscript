An identifier for a TweakDB record. Instances of this type can be created using the TweakDBID literal syntax, e.g. `t"AttachmentSlots.Head"`.

### Example

```
let isIconic = TweakDBInterface.GetBool(id + t".isPresetIconic", false);
```
